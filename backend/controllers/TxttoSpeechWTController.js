// LinguaLens/backend/controllers/TxttoSpeechWTController.js
// Lightweight stub for future enhancements (no history saving for now).

exports.handleTtsWithTranslate = (req, res) => {
  // This endpoint is intentionally minimal since translation+TTS
  // happens on the frontend in the current design.
  // You can extend this to run translation or TTS on the backend later.
  res.status(204).send(); // No Content
};
